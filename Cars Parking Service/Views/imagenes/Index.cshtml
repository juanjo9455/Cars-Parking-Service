@model IEnumerable<CarsParkingService.Models.imagenes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dato_imagen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ingreso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dato_imagen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ingreso.id_ingreso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_imagen">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_imagen">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_imagen">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

